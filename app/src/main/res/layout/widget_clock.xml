<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_clock_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_clock_background"
    android:clickable="true"
    android:focusable="true"
    android:gravity="top|start"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingTop="14dp"
    android:paddingEnd="20dp"
    android:paddingBottom="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">
            <TextClock
                android:id="@+id/widget_clock_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans"
                android:format12Hour="h:mm a"
                android:format24Hour="HH:mm"
                android:includeFontPadding="false"
                android:textColor="@color/widget_clock_text"
                android:textSize="36sp"
                android:textStyle="bold" />

            <TextClock
                android:id="@+id/widget_clock_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:fontFamily="sans"
                android:format12Hour="yyyy年M月d日（E）"
                android:format24Hour="yyyy年M月d日（E）"
                android:includeFontPadding="false"
                android:textColor="@color/widget_clock_muted"
                android:textSize="14sp" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/widget_menu_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/transparent"
            android:contentDescription="メニューを開く"
            android:padding="8dp"
            android:src="@drawable/ic_widget_menu" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_favorites_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="お気に入り"
        android:textColor="@color/widget_clock_text"
        android:textSize="12sp"
        android:textStyle="bold" />

    <GridView
        android:id="@+id/widget_favorites_grid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="4dp"
        android:clipToPadding="false"
        android:horizontalSpacing="2dp"
        android:numColumns="4"
        android:stretchMode="columnWidth"
        android:scrollbars="vertical"
        android:verticalSpacing="8dp" />

    <TextView
        android:id="@+id/widget_favorites_empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="「お気に入り」フォルダに登録したアプリがここに表示されます"
        android:textColor="@color/widget_clock_muted"
        android:textSize="12sp" />

</LinearLayout>
